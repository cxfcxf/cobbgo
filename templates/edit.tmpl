<html>
<body>

<h1>You are editing:</h1> 
<h3>{{ .Hostname }}</h3>

<form role="form" method="POST">
  <div class="col-sm-10">
    <input type="text" name="macaddr"  class="form-control" value="{{ .Info.macaddr}}">
  </div>

  <div class="col-sm-10">
    <input type="text" name="ksprofile"  class="form-control" value="{{ .Info.ksprofile}}">
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-primary">Submit</button>
    </div>
  </div>

</form>


</body>
</html>
